# ملاحظات التحقق

تم فحص النسخة المنشورة على `https://imadtbn.github.io/Manalsc/` قبل التعديل، وتبين أن الصفحة كانت تعرض عنوانًا عامًا فقط دون وسوم Open Graph أو Twitter أو canonical أو sitemap ظاهرة في المحتوى المستخرج.

تم فحص النسخة المحلية بعد التعديل في المتصفح، وتأكد ظهور عنوان الصفحة الجديد: `فروض واختبارات علوم الطبيعة للثانوي | منصة الأستاذة بوسنة منال`. كما ظهرت عناصر البحث والفلاتر مع تسميات وصول، وبطاقات الموارد مع أزرار معاينة وتحميل معنونة، إضافة إلى رابط تخطي المحتوى.

تم أيضًا التحقق برمجيًا من وجود `robots.txt` و`sitemap.xml` و`site.webmanifest` و`favicon.svg` و`og-image.png`، ومن صحة JSON-LD وصياغة JavaScript.
